query getPopularSearches($inputText: String) {
    xsearchPopularSearches(search: $inputText) {
        items {
            name
            url
            num_results
        }
        total_count
        code
    }
}
